# Hugo
/resources/
public/
assets/jsconfig.json
.hugo_build.lock

# macOS
.DS_Store

# Claude Code (machine-local settings)
.claude/

# Non-web image formats Hugo can't process (would break the gallery build)
*.heic
*.HEIC
