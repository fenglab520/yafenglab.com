# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Deploy Hugo site to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches:
      - main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

# Default to bash
defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.128.0
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb          
      - name: Install Dart Sass
        run: sudo snap install dart-sass
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"
      # --- Auto-generate the social-share (OG) card from the current homepage ---
      # Build a localhost copy (so its assets resolve locally), screenshot the
      # homepage at 1200x630, and write it to assets/media/sharing.png. The real
      # build below then picks it up as og:image. Both steps are non-fatal: if
      # anything fails, the committed assets/media/sharing.png is used instead, so
      # a deploy never breaks over the screenshot.
      - name: Build homepage copy for the OG snapshot
        continue-on-error: true
        env:
          HUGO_CACHEDIR: ${{ runner.temp }}/hugo_cache
        run: hugo --baseURL "http://localhost:8080/" -d /tmp/ogshot
      - name: Regenerate the OG card from the current homepage
        continue-on-error: true
        run: |
          (cd /tmp/ogshot && python3 -m http.server 8080 >/dev/null 2>&1 &)
          for i in $(seq 1 15); do curl -sf -o /dev/null http://localhost:8080/ && break; sleep 1; done
          google-chrome --headless=new --no-sandbox --disable-gpu --hide-scrollbars \
            --force-device-scale-factor=2 --window-size=1200,630 \
            --virtual-time-budget=12000 \
            --screenshot=/tmp/og-2x.png "http://localhost:8080/"
          convert /tmp/og-2x.png -resize 1200x630 assets/media/sharing.png
          echo "OG card regenerated:"; identify assets/media/sharing.png || true
      # --------------------------------------------------------------------------
      - name: Build with Hugo
        env:
          HUGO_CACHEDIR: ${{ runner.temp }}/hugo_cache
          HUGO_ENVIRONMENT: production
          TZ: America/Los_Angeles
        run: |
          hugo \
            --gc \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
